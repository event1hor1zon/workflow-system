# 项目架构书

## 1. 架构总览

系统采用前后端分离架构：

```text
浏览器
  ├── Vue 3 + Vite 前端
  │     ├── 登录页
  │     ├── 首页全景图
  │     ├── 工单列表页
  │     └── 工单详情页
  │
  └── REST API
        └── NestJS + TypeORM + PostgreSQL
```

前端负责交互、路由、主题、拓扑图展示；后端负责认证、角色权限、状态机、审批流、数据持久化。
当前版本还包含协同部门派单、部门处理完成回传和附件上传能力。

## 2. 前端架构

### 2.1 技术栈

- Vue 3
- Vite
- Vue Router 4
- Axios
- 原生 Web Speech API

### 2.2 页面结构

| 页面 | 说明 |
| --- | --- |
| `Login.vue` | 登录页 |
| `Panorama.vue` | 首页全景图 |
| `OrderList.vue` | 工单列表、筛选、新建工单 |
| `OrderDetail.vue` | 工单详情、流转、处理、审批 |

### 2.3 核心组件

| 组件 | 作用 |
| --- | --- |
| `TopologyGraph.vue` | 拓扑图 SVG 展示 |
| `OrderCard.vue` | 工单卡片 |
| `VoiceInput.vue` | 语音输入描述 |

### 2.4 前端状态

当前版本使用轻量的本地 store：

- `src/stores/user.js`
- 存储 `token`
- 存储 `user`
- 暴露角色判断和可创建工单判断

## 3. 后端架构

### 3.1 技术栈

- NestJS
- TypeORM
- PostgreSQL
- JWT
- PM2

### 3.2 模块划分

| 模块 | 职责 |
| --- | --- |
| `AuthModule` | 登录、JWT 校验、当前用户 |
| `UsersModule` | 用户查询 |
| `CountiesModule` | 旗县查询 |
| `DepartmentsModule` | 部门查询、核心部门列表 |
| `OrdersModule` | 工单状态机、流转、审批、详情 |

### 3.3 关键实体

#### `users`

- 用户账号
- 角色
- 所属旗县
- 所属部门

#### `counties`

- 旗县列表

#### `departments`

- 部门名称
- 部门类型

#### `orders`

- 工单标题/类型/优先级
- 发起人信息
- 当前处理旗县/部门
- 工单状态
- 协同部门任务
- 附件信息
- 发起人确认 / 领导确认标记

#### `order_flows`

- 工单流转历史
- 操作类型
- 来源部门 / 目标部门
- 操作人
- 备注

`action` 还会包含：

- `process`：市级定级与协同派单
- `complete`：协同部门处理完成
- `attachment`：上传附件

#### `order_approvals`

- 审批对象
- 审批类型
- 审批状态

## 4. 业务状态机

```text
普通员工发起
  -> pending
县级经办分配
  -> processing
市级部门定级并协同派单
  -> processing
协同部门处理完成
  -> waiting_confirm
确认完成
  -> completed

任一阶段可按权限退回
  -> rejected
```

## 5. 角色权限

| 角色 | 数据范围 | 动作 |
| --- | --- | --- |
| `user` | 自己发起的工单 | 创建、确认 |
| `county_handler` | 所属旗县工单 | 分配、驳回待分配工单 |
| `department_head` | 当前由本部门负责或协同参与的工单 | 定级、派单、处理完成、驳回处理中工单 |
| `top_leader` | 全部工单 | 查看、确认重大事件、驳回 |
| `admin` | 全部工单 | 查看、排障、辅助驳回 |

## 6. 审批机制

### 普通 / 紧急

1. 部门负责人完成处理
2. 系统创建发起人确认记录
3. 发起人确认后闭环

### 重大

1. 部门负责人完成处理，优先级为 `critical`
2. 系统创建两条审批记录
3. 发起人确认
4. 最高领导确认
5. 全部完成后闭环

## 7. 部署架构

```text
Nginx
  ├── /        -> 前端 dist 静态文件
  └── /api/*   -> 反向代理到 NestJS

PM2
  └── workflow-api

PostgreSQL
  └── workflow_db
```

## 8. 初始化策略

当前版本不依赖手写迁移文件，启动后端时通过以下机制完成初始化：

1. TypeORM 根据实体同步表结构
2. `SeedService` 在首次启动时写入：
   - `A县 / B县 / C县`
   - 三个核心部门
   - 管理员、领导、县级经办、部门负责人、普通员工测试账号

## 9. 当前部署约定

- 前端开发端口：`5173`
- 后端端口：`3000`
- 默认数据库：`workflow_db`
- 默认数据库用户：`workflow_user`
- 默认生产静态目录：`/var/www/workflow`
