# 项目架构书 — 企业工作流系统 v2.0

> 中国移动通信集团包头分公司 网络条线数字化协同项目

**版本**: v2.0
**编制日期**: 2026-04-08
**架构风格**: 前后端分离 SPA + RESTful API

---

## 一、系统架构总览

### 1.1 架构定位

本系统为**前后端分离架构**的企业级工作流管理系统：
- **前端**: Vue 3 SPA 单页应用
- **后端**: Node.js + Express RESTful API
- **数据库**: PostgreSQL
- **部署**: Nginx 静态托管 + 反向代理

```
┌─────────────────────────────────────────────────────────────────────┐
│                          用户层                                       │
│            PC浏览器 / 移动端浏览器（Chrome/Safari/Edge）              │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ HTTP/HTTPS (端口8080)
┌──────────────────────────────▼──────────────────────────────────────┐
│                      展示层 (Vue 3 SPA)                              │
│  ┌────────────┐  ┌────────────────┐  ┌──────────────────────────┐   │
│  │ Panorama   │  │ BusinessDemand  │  │      Login/Auth          │   │
│  │ 全景图视图  │  │   工单系统视图  │  │      用户认证视图        │   │
│  └────────────┘  └────────────────┘  └──────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              Vue Router（路由层）                             │   │
│  │  / → Panorama    /form → 工单列表    /form/:id → 详情       │   │
│  │  /login → 登录    /admin → 管理后台                         │   │
│  └──────────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │           Pinia（状态管理）                                   │   │
│  │  用户状态 / 工单状态 / 通知状态 / 主题状态                    │   │
│  └──────────────────────────────────────────────────────────────┘   │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ Axios HTTP Client
┌──────────────────────────────▼──────────────────────────────────────┐
│                      API网关层 (Nginx)                               │
│         静态资源托管 (端口8080)  |  API反向代理 (端口3000)            │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ HTTP (REST API)
┌──────────────────────────────▼──────────────────────────────────────┐
│                      服务层 (Express.js)                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐   │
│  │  认证中间件   │  │   路由模块    │  │       业务逻辑层         │   │
│  │  JWT验证     │  │  /api/xxx    │  │    Services / Controllers│   │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘   │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ Prisma ORM
┌──────────────────────────────▼──────────────────────────────────────┐
│                      数据层 (PostgreSQL)                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────────────┐   │
│  │  users   │  │ counties │  │departments│  │      orders        │   │
│  └──────────┘  └──────────┘  └──────────┘  └────────────────────┘   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────────────┐   │
│  │order_hist│  │ approvals│  │knowledge  │  │   notifications    │   │
│  └──────────┘  └──────────┘  └──────────┘  └────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、技术架构详解

### 2.1 前端技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 | 3.5+ |
| 语法 | Composition API + `<script setup>` | - |
| 构建 | Vite | 8.x |
| 路由 | Vue Router | 4.x |
| 状态 | Pinia | 2.x |
| HTTP | Axios | 1.x |
| 样式 | CSS Variables | - |

### 2.2 后端技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| 运行时 | Node.js | 20.x |
| 框架 | Express.js | 4.x |
| ORM | Prisma | 5.x |
| 数据库 | PostgreSQL | 15+ |
| 认证 | JWT | - |
| 加密 | bcrypt | - |
| 进程管理 | PM2 | - |

### 2.3 数据库设计

#### ER图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│  counties   │       │departments  │       │    users    │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id (PK)     │       │ id (PK)     │       │ id (PK)     │
│ name        │       │ name        │       │ username    │
│ description │       │ type        │       │ password    │
│ is_active   │       │ layer       │       │ name        │
└─────────────┘       │ icon        │       │ dept        │
       │             │ color       │       │ role        │
       │             └─────────────┘       │ is_active   │
       │                    │             └─────────────┘
       │                    │                    │
       │                    │                    │
       ▼                    ▼                    ▼
┌─────────────────────────────────────────────────────────────┐
│                         orders                              │
├─────────────────────────────────────────────────────────────┤
│ id (PK)          │ creator_id (FK)    │ dept_id (FK)      │
│ title             │ county_id (FK)      │ status            │
│ type              │ priority            │ approval_status   │
│ description       │ ai_judge_result     │ handlers          │
│ current_handler   │ start_time          │ reject_reason     │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│order_history │     │order_approvals│     │notifications  │
├───────────────┤     ├───────────────┤     ├───────────────┤
│ id (PK)       │     │ id (PK)        │     │ id (PK)       │
│ order_id (FK) │     │ order_id (FK)  │     │ user_id (FK)  │
│ action        │     │ approver_id    │     │ type          │
│ from_status   │     │ approver_type  │     │ title         │
│ to_status     │     │ status         │     │ is_read       │
│ operator      │     │ comment        │     │ order_id      │
└───────────────┘     └───────────────┘     └───────────────┘
```

#### 表结构详情

| 表名 | 说明 | 核心字段 |
|------|------|----------|
| users | 用户表 | id, username, password, name, dept, role |
| counties | 县级表 | id, name, description |
| departments | 部门表 | id, name, type, layer, icon |
| orders | 工单表 | id, title, status, priority, county_id |
| order_history | 流转记录 | id, order_id, action, operator |
| order_approvals | 审批记录 | id, order_id, approver_id, status |
| knowledge_base | 知识库 | id, category, keywords, content |
| sop_flows | SOP流程 | id, dept_id, direction, name, list |
| notifications | 通知表 | id, user_id, type, is_read |

---

## 三、组件架构

### 3.1 前端组件树

```
App.vue（根组件）
├── RouterView
│   ├── LoginView.vue（登录页）
│   ├── Panorama.vue（全景图）
│   │   ├── ThemeToggle.vue
│   │   └── [Modal] SOP详情弹窗
│   ├── BusinessDemand.vue（工单系统）
│   │   ├── ThemeToggle.vue
│   │   ├── [TabNav] 标签导航
│   │   ├── [OrderGrid] 工单卡片
│   │   ├── [NewOrderModal] 新建表单
│   │   └── [DetailPage] 详情页
│   │       └── [TopologyCanvas] SVG拓扑图
│   └── AdminView.vue（管理后台）
│       ├── UserManage.vue
│       ├── DeptManage.vue
│       └── OrderManage.vue
└── [Global] NotificationBell.vue
```

### 3.2 Pinia Store设计

```javascript
// stores/
├── auth.js        // 用户认证状态
├── orders.js      // 工单数据状态
├── notifications.js // 通知状态
└── theme.js       // 主题状态
```

---

## 四、API架构

### 4.1 API路由结构

```
/api/
├── auth/
│   ├── POST   /login          # 用户登录
│   ├── POST   /register       # 用户注册
│   └── GET    /me             # 获取当前用户
│
├── users/
│   ├── GET    /               # 获取用户列表
│   ├── GET    /:id            # 获取用户详情
│   ├── POST   /               # 创建用户
│   ├── PUT    /:id            # 更新用户
│   └── DELETE /:id            # 删除用户
│
├── orders/
│   ├── GET    /               # 获取工单列表
│   ├── GET    /:id            # 获取工单详情
│   ├── POST   /               # 创建工单
│   ├── PUT    /:id            # 更新工单
│   ├── DELETE /:id            # 删除工单
│   ├── POST   /:id/submit     # 提交工单
│   ├── POST   /:id/approve    # 审批工单
│   └── POST   /:id/reject     # 驳回工单
│
├── departments/
│   ├── GET    /               # 获取部门列表
│   └── GET    /:id            # 获取部门详情
│
├── counties/
│   ├── GET    /               # 获取县级列表
│   └── GET    /:id            # 获取县级详情
│
├── knowledge/
│   ├── GET    /               # 获取知识库
│   └── POST   /classify       # AI分类判断
│
├── notifications/
│   ├── GET    /               # 获取通知列表
│   └── PUT    /:id/read       # 标记已读
│
└── sop/
    └── GET    /flows          # 获取SOP流程
```

### 4.2 认证流程

```
用户登录
    │
    ▼
POST /api/auth/login
    │
    ├─── 验证用户名密码
    │
    ▼
生成 JWT Token (7天有效期)
    │
    ▼
返回 { token, user }
    │
    ▼
前端存储 Token (localStorage)
    │
    ▼
后续请求携带 Authorization: Bearer <token>
    │
    ▼
后端中间件验证 Token
    │
    ├─── 有效 → 请求继续
    │
    └─── 无效/过期 → 返回 401
```

---

## 五、部署架构

### 5.1 服务器环境

| 项目 | 内容 |
|------|------|
| 服务器 | Linux (Ubuntu 20.04+) |
| Web服务 | Nginx |
| 数据库 | PostgreSQL 15+ |
| Node.js | 20.x |
| 进程管理 | PM2 |
| 后端端口 | 3000 |
| 前端端口 | 8080 |

### 5.2 部署流程

```
开发环境（代码编辑）
    │
    ├── 前端 ─────────────────┐
    │   ↓ 修改 src/*.vue       │
    │   ↓ npm run dev          │
    │   ↓ npm run build        │
    │   ↓ dist/                │
    │                         │
    └── 后端 ─────────────────┘
        ↓ 修改 src/*.js
        ↓ npm run build
        ↓ dist/

构建产物
    │
    ├── 前端 dist/ → /var/www/workflow/dist/
    │
    └── 后端 dist/ → PM2 管理

Nginx 配置
    │
    ├── 端口8080 → 前端静态资源
    │
    └── /api → 反向代理到 127.0.0.1:3000

用户访问 http://服务器IP:8080
```

### 5.3 一键部署脚本

```bash
# scripts/deploy.sh 功能列表
1. 安装基础依赖 (curl, git, nginx, postgresql)
2. 安装 Node.js 20
3. 配置 PostgreSQL (创建数据库和用户)
4. 初始化数据库 (执行 init-db.sql 和 seed-data.sql)
5. 安装 Ollama (可选，AI功能)
6. 部署后端 (npm install, build, pm2)
7. 部署前端 (npm install, build, nginx配置)
```

---

## 六、安全架构

### 6.1 认证与授权

- JWT Token 认证 (7天有效期)
- 密码 bcrypt 加密存储
- RBAC 角色权限控制

### 6.2 角色说明

| 角色 | 权限说明 |
|------|----------|
| admin | 系统管理员，拥有所有权限 |
| super | 最高权限人，跨部门管理 |
| leader | 部门负责人，审批工单 |
| county | 县级经办人，属地管理 |
| user | 普通员工，创建和提交工单 |

### 6.3 安全措施

- CORS 跨域限制
- 请求频率限制
- SQL 注入防护 (Prisma ORM)
- XSS 防护 (前端转义)
- HTTPS 传输 (生产环境)

### 6.4 Nginx 安全头

```nginx
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-XSS-Protection "1; mode=block" always;
```

---

## 七、性能优化

### 7.1 前端优化

- Vite 构建优化 (代码分割、Tree Shaking)
- 静态资源缓存 (1年过期)
- Gzip 压缩
- 图片资源优化

### 7.2 后端优化

- 数据库索引优化
- 连接池管理
- 响应压缩

### 7.3 数据库索引

```sql
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_creator ON orders(creator_id);
CREATE INDEX idx_orders_handler ON orders(current_handler);
CREATE INDEX idx_history_order ON order_history(order_id);
CREATE INDEX idx_approvals_order ON order_approvals(order_id);
CREATE INDEX idx_notifications_user ON notifications(user_id);
```

---

## 八、扩展性设计

### 8.1 水平扩展

- 后端无状态设计，支持多实例部署
- PM2 集群模式
- Nginx 负载均衡

### 8.2 功能扩展

- AI 智能分类 (Ollama 集成)
- 邮件通知
- 短信通知
- 微信集成

### 8.3 数据扩展

- MongoDB 存储日志
- Redis 缓存热点数据
- Elasticsearch 全文搜索

---

## 九、关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 前后端分离 | SPA + REST API | 支持多端访问，便于前后端独立迭代 |
| Vue vs React | Vue 3 | 团队熟悉度，Composition API 灵活性 |
| Pinia vs Vuex | Pinia | 更轻量，TypeScript 支持更好 |
| Prisma vs TypeORM | Prisma | 类型安全，迁移方便 |
| PostgreSQL vs MySQL | PostgreSQL | JSONB 支持，丰富的数据类型 |
| PM2 vs Docker | PM2 | 简单部署，无需容器编排 |

---

## 十、项目目录结构

```
workflow/
├── backend/
│   ├── src/
│   │   ├── index.js           # 入口文件
│   │   ├── app.js             # Express应用
│   │   ├── config/
│   │   │   └── database.js    # 数据库配置
│   │   ├── controllers/
│   │   │   ├── authController.js
│   │   │   ├── orderController.js
│   │   │   ├── userController.js
│   │   │   └── ...
│   │   ├── routes/
│   │   │   ├── authRoutes.js
│   │   │   ├── orderRoutes.js
│   │   │   └── ...
│   │   ├── middlewares/
│   │   │   ├── authMiddleware.js
│   │   │   └── errorMiddleware.js
│   │   ├── services/
│   │   │   ├── orderService.js
│   │   │   └── ...
│   │   ├── models/
│   │   │   └── prismaClient.js
│   │   └── utils/
│   │       ├── jwt.js
│   │       └── ...
│   ├── prisma/
│   │   ├── schema.prisma      # 数据库Schema
│   │   └── migrations/        # 迁移文件
│   ├── dist/                  # 构建产物
│   ├── .env                   # 环境变量
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── App.vue
│   │   ├── main.js
│   │   ├── style.css
│   │   ├── api/
│   │   │   └── index.js       # Axios配置
│   │   ├── components/
│   │   │   ├── BusinessDemand.vue
│   │   │   ├── ThemeToggle.vue
│   │   │   ├── TopologyCanvas.vue
│   │   │   └── ...
│   │   ├── views/
│   │   │   ├── LoginView.vue
│   │   │   ├── Panorama.vue
│   │   │   └── ...
│   │   ├── router/
│   │   │   └── index.js
│   │   ├── stores/
│   │   │   ├── auth.js
│   │   │   ├── orders.js
│   │   │   └── ...
│   │   └── utils/
│   │       └── ...
│   ├── dist/                  # 构建产物
│   └── package.json
│
├── scripts/
│   ├── deploy.sh              # 一键部署脚本
│   ├── init-db.sql           # 数据库初始化
│   └── seed-data.sql         # 种子数据
│
├── configs/
│   └── nginx.conf            # Nginx配置
│
├── public/
│   └── ...
│
├── package.json
├── README.md
└── 项目架构书.md
```

---

## 附录：环境变量说明

### 后端 .env

```env
# 数据库
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=workflow_user
DB_PASSWORD=workflow_pass_2024
DB_DATABASE=workflow

# JWT
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d

# 服务
PORT=3000

# Ollama (可选)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b

# 前端地址
FRONTEND_URL=http://localhost:8080
```

---

*项目架构书版本：v2.0 | 编制日期：2026-04-08*
