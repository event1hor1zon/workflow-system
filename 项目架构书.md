# 项目架构书 - 企业工作流系统

**版本**: v2.0.0
**编制日期**: 2026-04-08
**架构风格**: 前后端分离 SPA + RESTful API

---

## 一、系统架构总览

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户层                                    │
│          PC浏览器 / 移动端浏览器（Chrome/Safari/Edge）           │
└────────────────────────────┬──────────────────────────────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
              ▼                             ▼
┌─────────────────────────┐   ┌─────────────────────────┐
│      前端 (Vue 3)        │   │      后端 (NestJS)       │
│  ┌───────────────────┐  │   │  ┌───────────────────┐  │
│  │  Panorma.vue     │  │   │  │  REST API         │  │
│  │  全景图首页       │  │   │  │  /api/orders      │  │
│  └───────────────────┘  │   │  │  /api/users       │  │
│  ┌───────────────────┐  │   │  │  /api/departments │  │
│  │  OrderList.vue   │  │   │  │  /api/counties    │  │
│  │  工单列表         │  │   │  └───────────────────┘  │
│  └───────────────────┘  │   └───────────┬─────────────┘
│  ┌───────────────────┐  │               │
│  │  OrderDetail.vue │  │               │ PostgreSQL
│  │  工单详情+拓扑图   │  │               │
│  └───────────────────┘  │               ▼
│  ┌───────────────────┐  │   ┌─────────────────────────┐
│  │ TopologyGraph.vue │  │   │      PostgreSQL          │
│  │ 椭圆形拓扑图组件   │  │   │  users, orders,       │
│  └───────────────────┘  │   │  counties, departments, │
│  ┌───────────────────┐  │   │  order_flows,         │
│  │ VoiceInput.vue    │  │   │  order_approvals      │
│  │ 语音输入组件       │  │   └─────────────────────────┘
└─────────────────────────┘
```

---

## 二、技术架构

### 2.1 前端技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | Vue 3 | Composition API + `<script setup>` |
| 构建 | Vite | 快速开发，热更新 |
| 路由 | Vue Router 4 | 页面导航 |
| HTTP | Axios | API请求 |
| 状态 | Vue Reactive | 组件内状态管理 |
| 语音 | Web Speech API | 浏览器原生语音识别 |

### 2.2 后端技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | NestJS | 企业级Node.js框架 |
| 语言 | TypeScript | 类型安全 |
| 数据库 | PostgreSQL | 关系型数据库 |
| ORM | TypeORM | 数据库ORM |
| 认证 | JWT | Token认证 |
| 进程 | PM2 | 进程管理 |

---

## 三、功能架构

### 3.1 核心流程

```
① 员工提交工单
   │
   ├── 文字输入
   ├── 语音输入（Web Speech API）
   └── status: pending

② 县级经办人处理
   │
   ├── 查看工单
   ├── 选择流转部门（可多选）
   │   ├── 网络部
   │   ├── 客户响应中心
   │   └── 工程建设部
   └── status: processing

③ 三部门流转（环形）
   │
   ├── 网络部 ←→ 客户响应中心
   │     ↕
   └── 工程建设部

④ 部门处理
   │
   ├── 选择事件类型
   │   ├── 普通事件
   │   ├── 紧急事件
   │   └── 重大事件（双签）
   └── status: pending_approval

⑤ 确认结束
   │
   ├── 普通/紧急 → 发起人确认 → 结束
   └── 重大 → 发起人+最高领导双签 → 结束
```

### 3.2 权限控制

| 角色 | 可看工单 | 可操作 |
|------|---------|--------|
| user | 自己发起的 | 提交、确认结束 |
| county_handler | 所属县的待处理工单 | 分配部门、流转 |
| department_head | 部门的工单 | 处理、流转 |
| top_leader | **所有工单** | 审批重大事件 |
| admin | 所有 | 系统管理 |

---

## 四、组织架构

### 4.1 组织架构图

```
┌─────────────────────────────────────────────────┐
│              各旗县分公司 (A县/B县/C县)          │
└───────────────────────┬─────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │市场经营部│  │政企客户部│  │客户服务中心│
   └────┬────┘  └────┬────┘  └────┬────┘
         └─────────────┼─────────────┘
                       │
         ┌─────────────┼─────────────┐
         │   三部门椭圆形流转区      │
         │                           │
         │   ┌───┐  ┌───┐  ┌───┐  │
         │   │网络│◄─┼─►──│客响│  │
         │   └───┘  └───┘  └───┘  │
         │      ↖─────┼─────↗     │
         │          ┌──┴──┐         │
         │          │工建 │          │
         │          └──┬──┘         │
         └─────────────┼─────────────┘
                       │
                       ▼
                  ┌──────────┐
                  │  闭环存档  │
                  └──────────┘
```

### 4.2 工单状态机

```
pending → processing → pending_approval → resolved
              ↓
          rejected
```

---

## 五、数据架构

### 5.1 数据库表

| 表名 | 说明 | 核心字段 |
|------|------|----------|
| users | 用户表 | id, username, password, name, role, county_id, department_id |
| counties | 县级表 | id, name |
| departments | 部门表 | id, name, type |
| orders | 工单表 | id, title, status, event_type, creator_confirmed, leader_confirmed |
| order_flows | 流转记录 | id, order_id, action, from_department, to_department |
| order_approvals | 审批记录 | id, order_id, user_id, approval_type, status |

---

## 六、组件架构

### 5.1 前端组件树

```
App.vue
├── router-view
│   ├── Login.vue (登录页)
│   ├── Panorama.vue (全景图首页)
│   │   └── TopologyGraph.vue (椭圆形拓扑图)
│   ├── OrderList.vue (工单列表)
│   │   └── OrderCard.vue (工单卡片)
│   │   └── CreateOrder.vue (创建对话框)
│   │       └── VoiceInput.vue (语音输入)
│   └── OrderDetail.vue (工单详情)
│       ├── TopologyGraph.vue (椭圆形拓扑图)
│       └── 流转历史
└── ThemeToggle.vue (主题切换)
```

### 5.2 椭圆形拓扑图设计

```
                    发起人
                      │
                      ▼
              ┌───────────────┐
              │ 椭圆形流转区  │
              │               │
              │  ┌───┐ ┌───┐  │
              │  │网络│─│客响│  │
              │  └───┘ └───┘  │
              │     │    │     │
              │     └──┬─┘     │
              │        │       │
              │      ┌──┴──┐    │
              │      │工建 │    │
              │      └─┬──┘    │
              │        │       │
              └────────┼───────┘
                       │
                       ▼
                  ┌──────────┐
                  │ 闭环存档  │
                  └──────────┘
```

---

## 七、API架构

### 7.1 端点列表

| 资源 | 端点 | 方法 | 说明 |
|------|------|------|------|
| 认证 | /api/auth/login | POST | 登录 |
| 用户 | /api/users | GET | 用户列表 |
| 用户 | /api/users/leaders | GET | 领导列表 |
| 县级 | /api/counties | GET | 县级列表 |
| 部门 | /api/departments | GET | 部门列表 |
| 部门 | /api/departments/transferable | GET | 三流转部门 |
| 工单 | /api/orders | GET/POST | 工单列表/创建 |
| 工单 | /api/orders/:id | GET | 工单详情 |
| 工单 | /api/orders/:id/topology | GET | 拓扑图数据 |
| 工单 | /api/orders/:id/flows | GET | 流转历史 |
| 工单 | /api/orders/:id/assign | PATCH | 分配部门 |
| 工单 | /api/orders/:id/transfer | POST | 部门流转 |
| 工单 | /api/orders/:id/process | PATCH | 处理工单 |
| 工单 | /api/orders/:id/confirm | PATCH | 确认结束 |
| 工单 | /api/orders/:id/reject | PATCH | 退回 |

### 7.2 认证流程

```
用户登录 → POST /api/auth/login → 验证密码 → 返回JWT
                                      ↓
                              后续请求携带Token
                                      ↓
                              后端验证Token → 继续/401
```

---

## 八、部署架构

### 8.1 端口分配

| 端口 | 服务 | 说明 |
|------|------|------|
| 8080 | Nginx | 前端静态资源 |
| 3000 | NestJS | 后端API |
| 5432 | PostgreSQL | 数据库 |

### 8.2 一键部署

```bash
git clone https://github.com/event1hor1zon/workflow-system.git
cd workflow-system
chmod +x scripts/deploy.sh
sudo ./scripts/deploy.sh
```

---

## 九、目录结构

```
工作流/
├── backend/                    # NestJS 后端
│   └── src/
│       ├── auth/            # 认证模块
│       ├── users/           # 用户模块
│       ├── counties/        # 县级模块
│       ├── departments/    # 部门模块
│       └── orders/          # 工单模块
│
├── src/                      # Vue3 前端
│   ├── api/                 # API服务层
│   ├── components/          # 组件
│   │   ├── TopologyGraph.vue # 椭圆形拓扑图
│   │   └── VoiceInput.vue   # 语音输入
│   └── views/              # 页面
│
├── scripts/                 # 部署脚本
│   ├── deploy.sh           # 一键部署
│   ├── init-db.sql         # 数据库初始化
│   └── seed-data.sql       # 种子数据
│
└── configs/
    └── nginx.conf         # Nginx配置
```

---

*文档版本：v2.0.0 | 更新日期：2026-04-08*
