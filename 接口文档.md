# 接口文档 - SOP全景图网站

## 概述

> ⚠️ **说明**：本项目为纯前端单页应用（SPA），无后端API接口。所有数据存储在浏览器内存中，页面刷新后数据会重置。

本文档记录的是：
1. **前端路由接口**（页面导航）
2. **组件间数据接口**（Props/Emits/Provide/Inject）
3. **内置数据结构**（工单数据模型）

---

## 1. 路由接口

### 1.1 路由配置

```javascript
// 路由模式: Web History
// Base Path: /

const routes = [
  {
    path: '/',
    name: 'panorama',
    component: Panorama,
    meta: { title: '握手协同SOP全景图' }
  },
  {
    path: '/form',
    name: 'business-demand-list',
    component: BusinessDemand,
    meta: { title: '业务需求工单' }
  },
  {
    path: '/form/:orderId',
    name: 'business-demand-detail',
    component: BusinessDemand,
    meta: { title: '工单详情' }
  }
]
```

### 1.2 路由导航

| 操作 | 代码 | 说明 |
|------|------|------|
| 跳转全景图 | `router.push('/')` | 返回首页 |
| 跳转工单列表 | `router.push('/form')` | 打开工单列表 |
| 跳转工单详情 | `router.push('/form/' + orderId)` | 打开指定工单 |
| 返回上一页 | `router.back()` | history.back() |

### 1.3 路由参数

| 路由路径 | 参数名 | 类型 | 说明 |
|---------|-------|------|------|
| `/form/:orderId` | orderId | string | 工单ID，格式：`REQ-YYYYMMDD-NNN` |

---

## 2. 组件数据接口

### 2.1 ThemeToggle 组件

```typescript
// Props: 无

// Emits: 无 (直接修改CSS变量)

// 使用方法:
// <ThemeToggle />
// 点击切换 --theme 属性
```

### 2.2 工单数据模型

```typescript
interface Order {
  id: string              // 工单ID，如 "REQ-20260402-001"
  title: string           // 需求标题
  creator: string         // 发起人
  dept: string            // 发起部门
  type: string            // 需求类型
  createTime?: string     // 创建时间 (新建工单)
  saveTime?: string       // 保存时间 (草稿工单)
  startTime?: string      // 发起时间 (流转中工单)
  usedTime?: string       // 已用时间
  currentNode?: string   // 当前环节
  progress?: number       // 进度百分比 (0-100)
  statusClass: string     // 状态类: 'new' | 'draft' | 'flowing'
  statusText: string      // 状态文本
  desc?: string           // 需求描述
  priority?: string       // 优先级: 'normal' | 'urgent' | 'critical'
  priorityText?: string   // 优先级文本
  topology?: TopologyNode[]  // 闭环拓扑图节点
  resolveStatus?: string  // 解决状态: 'resolved' | 'partial' | 'unresolved'
  remainingPart?: string  // 剩余部分
  resolveNote?: string    // 解决备注
}

interface TopologyNode {
  type: string            // 节点类型: 'start' | 'node' | 'end'
  name: string             // 节点名称
  icon: string             // 图标emoji
  handler: string          // 责任人
  statusText: string       // 状态文本
  statusClass: string      // 状态类: 'completed' | 'processing' | 'pending'
  isCompleted: boolean     // 是否已完成
  isCurrent: boolean       // 是否当前节点
  totalTime?: string       // 总时长
  usedTime?: string        // 已用时长
  resolved?: boolean       // 是否已解决
  remaining?: string       // 剩余流转
  factors?: string         // 不具备条件因素
}
```

### 2.3 部门数据模型

```typescript
interface Department {
  id: string      // 部门ID，如 'gongjian', 'wangluo', 'kexiang'
  name: string     // 部门名称
  icon: string     // 部门图标emoji
  color: string    // 颜色类: 'blue' | 'purple' | 'green'
}

interface UpperDept {
  name: string     // 上级部门名称
  type: string     // 固定为 'upper'
}

interface LowerDept {
  id: string       // 固定为 'county'
  name: string     // 固定为 '各旗县分公司'
  type: string     // 固定为 'lower'
}
```

---

## 3. 新建表单接口

### 3.1 表单字段

```typescript
interface NewForm {
  title: string    // 需求标题
  type: string      // 需求类型
  dept: string      // 发起部门
  creator: string   // 联系人
  desc: string      // 需求描述
  priority: string  // 优先级: 'normal' | 'urgent' | 'critical'
}
```

### 3.2 表单选项

```typescript
// 需求类型选项
const TYPE_OPTIONS = [
  '5G基站建设',
  '家宽建设',
  '政企专线',
  '沿街商铺',
  '垂直行业',
  '集家客维护',
  '现网资源维护'
]

// 部门选项
const DEPT_OPTIONS = [
  '市场经营部',
  '政企客户部',
  '客户服务中心',
  '网络部',
  '工程建设部',
  '客户响应中心'
]

// 优先级选项
const PRIORITY_OPTIONS = [
  { value: 'normal', label: '一般' },
  { value: 'urgent', label: '紧急' },
  { value: 'critical', label: '重大' }
]
```

---

## 4. 弹窗数据接口

### 4.1 弹窗数据结构

```typescript
interface ModalData {
  // 核心部门
  id?: string        // 'gongjian' | 'wangluo' | 'kexiang'
  name?: string      // 部门名称
  icon?: string      // 图标
  color?: string     // 颜色
  
  // 上级部门
  type?: string      // 'upper' | 'lower'
  
  // 中心闭环
  id === 'center'
}
```

---

## 5. 内置示例数据

### 5.1 工单示例数据

```typescript
// 新建工单
const newOrders = [
  {
    id: 'REQ-20260402-001',
    title: '政企客户部办公楼5G室分覆盖需求',
    creator: '李明',
    dept: '政企客户部',
    type: '5G基站建设',
    createTime: '2026-04-02 09:30',
    statusClass: 'new',
    statusText: '新建'
  }
]

// 流转中工单 (含拓扑图)
const flowingOrders = [
  {
    id: 'REQ-20260328-008',
    title: '某工业园区5G基站建设需求',
    statusClass: 'flowing',
    topology: [
      { type: 'start', name: '政企客户部', icon: '🏢', statusClass: 'completed' },
      { type: 'node', name: '网络部', icon: '🌐', statusClass: 'processing', isCurrent: true },
      { type: 'node', name: '工程建设部', icon: '🏗️', statusClass: 'pending' },
      { type: 'node', name: '客户响应中心', icon: '📞', statusClass: 'pending' },
      { type: 'end', name: '闭环', icon: '✅', statusClass: 'resolved' }
    ]
  }
]
```

---

## 6. 主题接口

### 6.1 CSS 变量

```css
:root {
  --bg-primary: #0f172a;      /* 主背景 */
  --bg-secondary: #1e293b;   /* 次级背景 */
  --bg-card: #1e293b;       /* 卡片背景 */
  --text-primary: #f1f5f9;  /* 主文字 */
  --text-secondary: #94a3b8;/* 次级文字 */
  --accent: #3b82f6;         /* 强调色-蓝 */
  --accent-bg: rgba(59, 130, 246, 0.1);  /* 强调背景 */
  --border-color: #334155;   /* 边框色 */
  --shadow: rgba(0, 0, 0, 0.5); /* 阴影 */
}
```

### 6.2 主题切换

```javascript
// 通过设置 data-theme 属性切换
document.documentElement.setAttribute('data-theme', 'light')   // 亮色
document.documentElement.setAttribute('data-theme', 'dark')    // 暗色
```

---

## 7. 错误处理

> ⚠️ 本项目为纯前端项目，无后端API，以下仅为前端状态错误处理说明。

### 7.1 前端错误状态

| 场景 | 处理方式 |
|------|---------|
| 工单ID不存在 | 留在列表页 |
| 表单验证失败 | 阻止提交，显示错误提示 |
| 路由不存在 | Vue Router 默认处理 |

---

*文档更新时间：2026-04-03*
