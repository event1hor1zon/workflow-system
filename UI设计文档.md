# 企业工作流系统 UI 设计文档

> 版本：v3.0  
> 更新日期：2026-04-22  
> 参考来源：`/Users/a1234/Desktop/enterprise-workflow-engine`

## 1. 设计目标

本次 UI 重构的目标不是单独优化某一个页面，而是把整套前端统一到同一种“企业级工作台”语言中，同时严格适配现有工单业务流程。

本次重构后的核心目标：

- 首页最醒目的位置必须是“新建工单”
- 工单详情、审批状态、处理轨迹、可执行动作全部收拢到个人中心
- 登录后按工号识别员工档案，不在前端手动选择所属公司
- 保留你定义的实际业务路径：员工发起 -> 公司网络部负责人审核 -> 市级三部门定级处理 -> 发起人/最高领导确认闭环
- 市级负责人定级后可选择一个或多个协同部门，也可以不选；当前部门处理完成后再回到发起人确认
- 创建工单和工单详情都支持附件上传，分别用于问题资料和处理证明
- 最高领导可查看全部工单

## 2. 设计来源与映射关系

本次前端视觉和布局结构参考了 `enterprise-workflow-engine`，但不是机械复制，而是按我们当前 Vue 项目与真实接口重新落地。

### 参考页面映射

| 参考页面 | 当前落地页面 | 说明 |
| --- | --- | --- |
| `Login.tsx` | `src/views/Login.vue` | 保留玻璃卡片、液态背景、科技化登录氛围，增加密码输入 |
| `Dashboard.tsx` | `src/views/Panorama.vue` | 首页改造成强入口控制台，顶部主视觉突出“新建工单” |
| `TicketList.tsx` | `src/views/OrderList.vue` 左侧 | 个人中心左侧改为工单流式列表 |
| `TicketDetail.tsx` | `src/views/OrderList.vue` 右侧 | 工单详情、动作区、审批状态和处理轨迹合并展示 |
| `CreateTicket.tsx` | `src/views/OrderList.vue` 创建层 | 新建工单采用沉浸式全屏提单层，保留只填详情的业务规则 |
| `Sidebar.tsx + Topbar.tsx` | `src/components/WorkspaceFrame.vue` | 提炼成统一壳层，应用于首页和个人中心 |

## 3. 页面结构

### 3.1 登录页

文件：`src/views/Login.vue`

结构：

- 液态背景 + 网格背景
- 右上角主题切换
- 左侧品牌说明区
- 右侧授权登录卡片
- 底部测试账号和角色提示

适配点：

- 目标 UI 原始版本只输入工号；当前系统保留密码登录
- 保留“工号即身份识别”的表达，便于后续接入正式员工登记表

### 3.2 首页控制台

文件：`src/views/Panorama.vue`

结构：

- `WorkspaceFrame` 页面壳
- Hero 主视觉区：大标题 + 强 CTA + 系统摘要
- 核心指标区
- 固定流转路径区
- 部门职能矩阵区
- 办理须知区

业务表达重点：

- 新建工单必须是首屏第一入口
- 首屏文案明确说明“只填详情描述、自动识别所属公司、自动流转”
- 部门介绍放在下方，符合“先提单、再看说明”的操作优先级

### 3.3 个人中心 / 工单工作台

文件：`src/views/OrderList.vue`

结构：

- Hero 信息条：身份、所属公司、新建工单入口
- 统计卡片：可见工单、待我处理、已完成、已驳回
- 左侧工单列表
- 右侧详情与动作面板
- 全屏创建层

右侧详情面板包含：

- 工单主信息
- 当前环节 / 当前部门 / 工单级别 / 确认规则
- 当前可执行操作
- 工单描述
- 协同部门任务状态
- 附件区域：问题附件、处理证明
- 审批状态
- 处理轨迹

### 3.4 创建工单层

文件：`src/views/OrderList.vue`

结构：

- 全屏遮罩层
- 自动识别身份信息卡片
- 仅保留“工单详情描述”
- 使用 `VoiceInput` 支持语音转文字
- 支持上传问题附件
- 底部固定提交区

业务规则：

- 不再手动选择公司
- 不再手动选择部门
- 系统自动按员工档案识别所属公司

## 4. 统一壳层规范

文件：`src/components/WorkspaceFrame.vue`

统一壳层负责：

- 左侧侧边导航
- 顶部标题栏
- 主题切换
- 当前用户信息展示
- 全局背景氛围

当前导航只保留两个高频入口：

- 首页控制台
- 个人中心 / 工单

原因：

- 当前产品核心操作只有“发起工单”和“查看/办理工单”
- 避免堆砌无业务承载的假导航项

## 5. 视觉系统

### 5.1 视觉关键词

- 企业级
- 科技感
- 统一壳层
- 强卡片化
- 高信息密度但不压迫

### 5.2 颜色策略

主色：

- `--brand-primary`: 蓝色，用于主要 CTA、焦点、关键状态
- `--brand-secondary`: 绿色，用于完成、确认成功
- `--brand-warning`: 橙色，用于待处理、紧急
- `--brand-danger`: 红色，用于驳回、重大

背景：

- 浅色主题默认启用，便于企业办公场景长期使用
- 深色主题作为可选模式保留
- 页面使用径向渐变 + tech-grid 网格纹理增强层次

### 5.3 字体策略

主字体：

- `Inter`
- 中文回退：`PingFang SC / Microsoft YaHei / Noto Sans SC`

等宽字体：

- `JetBrains Mono`

使用规则：

- 页面标题采用紧凑字距和高字重
- 编号、工单 ID、系统状态采用等宽字体

## 6. 核心组件规范

### 6.1 OrderCard

文件：`src/components/OrderCard.vue`

用途：

- 列表中的单条工单摘要卡片

内容结构：

- 状态标签
- 工单编号
- 级别标签
- 标题
- 描述摘要
- 发起人 / 所属公司 / 当前环节
- 时间与“查看详情”提示

交互：

- Hover 提升阴影与边框强调
- 选中状态由父层通过 `.selected` 注入

### 6.2 Action Card

文件：`src/views/OrderList.vue`

用途：

- 承载不同角色的当前可执行动作

类型：

- 提交到市级部门
- 驳回工单
- 选择工单级别并派发协同部门
- 协同部门处理完成
- 上传附件
- 确认闭环

设计原则：

- 每种操作一个独立卡片
- 操作说明必须直接写在卡片里
- 颜色与操作性质对应

### 6.3 Status Pill

适配状态：

- `pending`
- `processing`
- `waiting_confirm`
- `completed`
- `rejected`

视觉要求：

- 标签一眼能区分，不靠文字单独识别
- 工单级别标签和状态标签独立展示，避免混淆

## 7. 业务适配规则

UI 必须服从当前业务流程，不允许为了“像参考站”而破坏工单流转。

### 7.1 新建工单

- 首页和个人中心都提供“新建工单”入口
- 表单只保留详情描述
- 提交后自动流转到所属公司网络部负责人

### 7.2 公司网络部负责人

- 在个人中心看到待审核工单
- 可执行两个动作：
  - 驳回
  - 提交到市级部门

### 7.3 市级三部门负责人

- 在个人中心看到待处理工单
- 接单后选择工单级别：
  - 普通
  - 紧急
  - 重大
- 可选择一个或多个协同部门一并派单，也可以不选协同部门
- 协同部门处理完成后，工单才回到发起人确认
- 重大工单需要发起人和最高领导双签确认
- 可上传处理结果证明附件

### 7.4 发起人 / 最高领导确认

- 普通和紧急工单：发起人确认即可闭环
- 重大工单：发起人和最高领导全部确认才闭环
- 最高领导能查看所有工单

## 8. 响应式策略

桌面端：

- 保留左侧侧边栏 + 顶部栏 + 内容区三段式布局

平板端：

- 侧边栏折叠为上方区块
- 首页 Hero 改为单列

移动端：

- 首页、工作台、创建层全部改为单列
- 统计卡片、过滤标签和动作卡片垂直堆叠
- 提交按钮和主要操作按钮拉满宽度

## 9. 主题切换策略

主题变量定义在：

- `src/style.css`

切换入口：

- 登录页右上角
- 壳层顶部栏

规则：

- 默认浅色模式
- 使用 `localStorage` 持久化 `sop-theme`

## 10. 维护建议

后续如果继续扩展 UI，建议遵守以下原则：

1. 先扩展 `WorkspaceFrame`，不要给每个页面单独造壳层。
2. 新的流程操作优先复用“动作卡片”模式，不要把表单散落到多个区域。
3. 工单详情继续收敛在个人中心，不再恢复成独立详情页。
4. 员工档案正式接入后，只替换登录查表和用户信息来源，不改前端提单字段结构。
5. 若继续对齐参考项目视觉，可优先补动画细节，而不是改动当前业务信息层级。
